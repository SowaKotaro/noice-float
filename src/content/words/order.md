---
term: "order"
reading: "おーだー"
aliases: ["オーダー", "順序", "並び順", "ORDER BY"]

engineer:
  levels:
    - "並び順のこと。取り出したデータをどう並べるかを指定する。"
    - "並び順。SQL では ORDER BY で「この列の昇順で」と指定する。指定しなければ順番は保証されず、同じ問い合わせでも毎回違う順で返ってくることがある。"
    - "順序。リレーショナルデータベースのテーブルは集合なので行に本来の順番はなく、ORDER BY を書かないかぎり返却順は不定（実行計画やストレージの都合で変わる）。ページングでソートキーの値が同点になる行があると、ページをまたいだ取りこぼしや重複が起きるため、並び順には必ず一意な列（主キーなど）をタイブレークとして足しておく。"
  examples:
    - "order by 付け忘れてるから順番バラバラだよ"
    - "並び順は id で order 固定して"
    - "その API、order は保証されてないよ"

general:
  meaning: "飲食店などで注文すること。また、注文したもの。"
  examples:
    - "オーダーお願いします"
    - "ラストオーダーは 22 時です"
    - "さっきのオーダー、まだ通ってない？"

tags: ["データ", "買い物"]
publishedAt: 2026-07-24
---
